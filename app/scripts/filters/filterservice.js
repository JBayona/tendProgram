'use strict';

/**
 * @ngdoc filter
 * @name tendProgramApp.filter:filterService
 * @function
 * @description
 * # filterService
 * Filter in the tendProgramApp.
 */
angular.module('tendProgramApp')
  .filter('filterService', function () {
    return function (input) {
    	
      //return 'filterService filter: ' + input;
    };
  });
